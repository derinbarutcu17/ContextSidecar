{
  "mcpServers": {
    "context-sidecar": {
      "command": "./pnpm",
      "args": ["exec", "context-sidecar", "serve", "mcp"],
      "env": {
        "CONTEXT_SIDECAR_HOME": "./.context-sidecar"
      }
    }
  }
}
