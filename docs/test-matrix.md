# Test Matrix

- Domain: schema validation
- Storage: create, update, get, list, search, archive, pin, expired handling
- Core: deterministic ranking and rendered pack output
- MCP: tool listing, bootstrap/readiness smoke tests, fresh-run resource visibility, and run-result linkage
- CLI: JSON-mode context flow, doctor, demo, setup, dev, and agent config entrypoints
- HTTP: create, get, list, search, pack, archive, pin
- Eval: realistic inclusion, exclusion, ordering, and rendered text checks
