# Release Checklist

- README matches actual behavior
- `./pnpm setup`, `./pnpm doctor`, `./pnpm demo`, and `./pnpm dev` are the obvious root commands
- Agent config snippets are generated by the repo itself
- MCP is the primary agent interface; CLI and HTTP support the same core service
- The MCP bootstrap contract and smoke-test sequence are documented and pass in a fresh client session
- archived and expired handling is consistent
- ranking rules are documented and tested
- typecheck passes
- tests pass
- eval fixtures pass
